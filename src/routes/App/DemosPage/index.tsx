import styles from "./DemosPage.module.scss";

const DemosPage: React.FC = () => {
    return (
        <div className={styles.root}>
            Nothing here yet
        </div>
    );
};

export {DemosPage};